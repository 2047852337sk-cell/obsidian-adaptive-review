# 参与贡献

欢迎提交可复现的问题和小范围改进。请在独立示例库操作，不要把私人笔记库直接提交到本仓库。

1. Fork 并创建分支。
2. 修改模板、对应视图和说明；涉及路径或字段变更时检查所有引用。
3. 使用 Node.js 20+ 运行 `npm test` 和 `npm run check`。
4. 在 Obsidian 中验证显示与按钮行为，提交 PR 并注明验证环境和未验证项。

调度调整应给出具体的评分前后状态，以及低分、连续高分、最后阶段的预期结果。避免只改变一个面板造成行为不一致。

提交前检查暂存文件列表，不提交真实复习历史、分析报告、插件配置、凭据或原笔记库的 Git 历史。Issue 中请使用虚构卡片和脱敏错误信息。
