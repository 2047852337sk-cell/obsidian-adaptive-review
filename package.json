{
  "name": "obsidian-adaptive-review",
  "version": "0.1.0",
  "private": true,
  "description": "An Obsidian vault workflow for adaptive spaced review",
  "license": "MIT",
  "scripts": {
    "test": "node --test tests/*.test.cjs",
    "check": "node scripts/check.cjs"
  },
  "engines": {
    "node": ">=20"
  }
}
